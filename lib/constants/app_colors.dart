import 'package:flutter/material.dart';

class AppColors {
  // 🖤 Base colors
  static const Color black = Color(0xFF1A1A1A);
  static const Color white = Color(0xFFFFFFFF);

  // 🌿 Background (soft vintage white)
  static const Color background = Color(0xFFF7F7F5);

  // ♻️ Recycling accent colors (muted for modern UI)

  // Green → Eco / primary actions
  static const Color green = Color(0xFF4CAF6A);

  // Blue → Info / secondary actions
  static const Color blue = Color(0xFF4A90E2);

  // Yellow → Highlights / tags
  static const Color yellow = Color(0xFFF4C542);

  // ⚪ Neutral grays (very important for UI polish)
  static const Color darkGray = Color(0xFF4B4B4B);
  static const Color lightGray = Color(0xFFBDBDBD);
  static const Color extraLightGray = Color(0xFFEAEAEA);
}